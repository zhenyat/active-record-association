%h1= t Doctor.model_name.human(count: 2)
%table.table.table-hover
  %thead
    %tr
      %th= Doctor.human_attribute_name(:name)
      %th= t 'status.status'
      %th= t 'actions.actions'

  %tbody
    - @doctors.each do |doctor|
      %tr
        %td= doctor.name
        %td= render 'admin/shared/index_buttons', resource: doctor
%br
.row
  .col-md-2
    = link_to t('actions.create'), new_admin_doctor_path, class: 'btn btn-primary btn-sm'
